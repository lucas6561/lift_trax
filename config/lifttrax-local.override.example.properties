# Copy this file to lifttrax-local.override.properties for machine-specific settings.
# The override file is ignored by Git and is layered over lifttrax-local.properties.
# Reuse the ignored hosted config for JDBC URL, username, and password.
lifttrax.config.include=lifttrax-hosted.properties
lifttrax.cli.userId=YOUR_USER_ID
