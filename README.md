# lift_trax

This repository contains implementations of a command-line application for recording lifts.

- `rust/` – **Archived** original implementation (source retained, no longer actively developed). See `rust/README.md` for historical build and usage details.
- `java/` – Incremental Java port using Gradle. See `java/README.md`.
