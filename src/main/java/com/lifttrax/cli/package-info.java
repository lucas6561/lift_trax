/**
 * Command-line and web-entry classes for LiftTrax.
 *
 * <p>Think of this package as the "front door" to the app:
 *
 * <ul>
 *   <li>CLI classes read command arguments and start features.
 *   <li>Web classes handle HTTP requests and build HTML responses.
 *   <li>Utility helpers here keep user input safe (escaping) and understandable.
 * </ul>
 */
package com.lifttrax.cli;
