/**
 * Command-line and web-entry classes for LiftTrax.
 *
 * <p>Think of this package as the "front door" to the app:</p>
 * <ul>
 *   <li>CLI classes read command arguments and start features.</li>
 *   <li>Web classes handle HTTP requests and build HTML responses.</li>
 *   <li>Utility helpers here keep user input safe (escaping) and understandable.</li>
 * </ul>
 */
package com.lifttrax.cli;
